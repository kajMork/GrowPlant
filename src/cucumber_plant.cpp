#include "../include/cucumber_plant.h"

void CucumberPlant::grow(int days) {
    watercoeficient = soil_moisture*(-1/5*height^2)
    lightcoeficient = 
    height = height + days * growth_rate * watercoeficient * lightcoeficient;
}